[tool.poetry]
name = "Exercise-8"
version = "0.1.0"
description = "Example exercises"
authors = ["Julia Wiener"]

[tool.poetry.dependencies]
python = "^3.8"
pytest = "^6.2.4"
flake8 = "^3.9.2"
black = "^21.5b1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
