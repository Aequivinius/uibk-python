[tool.poetry]
name = "exercise-8"
version = "0.1.0"
description = "initialising poetry"
authors = ["kickygit"]

[tool.poetry.dependencies]
python = "^3.8"
black = "^21.5b1"
pytest = "^6.2.4"
flake8 = "^3.9.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
