[tool.poetry]
name = "ex_7_enviorments"
version = "0.1.0"
description = "using poetry"
authors = ["Vizcachera <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
library = "^0.0.0"
pytest = "^6.2.4"
black = "^21.5b1"
flake8 = "^3.9.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
